import { StyleSheet } from 'react-native';

export const workoutFrequencyScreenStyles = StyleSheet.create({
    grid: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        justifyContent: 'space-between',
        gap: 14,
    },
    gridItem: {
        width: '47.5%',
    },
});
